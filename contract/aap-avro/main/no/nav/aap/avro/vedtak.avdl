@namespace("no.nav.aap.avro")
protocol VedtakV1 {
	import idl "medlem.avdl";

	enum Status {VEDTATT, AVSLÅTT, AVVENTER}

	record Vedtak {
		string personident;
		union{null, ErMedlem} erMedlem;
		Status status;
	}
}
